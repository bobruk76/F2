# F2


Необходимо установить docker и docker-compose.
Собрать образы командой
```
    docker-compose build
```
Их запустить с помощью команды 
```
    docker-compose up
```
Открыть ссылку
```
    http://localhost:9081/ 
```
Должно заработать...
Логины и пароли:
```
admin: admin
testuser: tstsr_psw
```

Первоначально планировалось использовать Postgres, 
но в последствии решил попробовать связку Django REST + MongoDB
(по-этому возможны некоторые SQL вставки, там где они не нужны)
Коннектор Djongo тоже заработал не сразу(пришлось понижать версию, 
чтобы можно было использовать административную панель). 
